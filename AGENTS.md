# AGENTS.md

## 0. このファイルの目的

このリポジトリは、鉄道乗務員が iPad で使用する乗務支援 Web アプリです。
Codex は、作業を始める前にこの `AGENTS.md` を読み、さらに必ず次の 2 ファイルも読んでください。

- `MUST_FOLLOW.md` — ユーザーが過去に「必ず守ってほしい」と明示した開発・変更ルール
- `HANDOFF.md` — 現行仕様、実装経緯、採用済み設計、既知課題、テスト観点

この 3 ファイルは同じ重要度ではありません。優先順位は次のとおりです。

1. その時点でユーザーが明示した最新の指示
2. `MUST_FOLLOW.md`
3. この `AGENTS.md`
4. 現在の実コード・データが示す現行挙動
5. `HANDOFF.md` の仕様説明
6. 過去版・バックアップ・旧コメント

文書と現行コードが食い違う場合、勝手にどちらかへ寄せないでください。
まず差分を確認し、ユーザーの最新指示がない場合は「現行挙動を壊さない」側を優先してください。
安全・業務挙動に関わる差分は、変更前にユーザーへ明示してください。

---

## 1. プロジェクト概要

### 用途
西武鉄道の乗務支援を想定した、スマートフォン／iPad 向け Web アプリ。

主な機能:

- GPS による現在位置・駅間・接近判定
- 次停車駅、停車、通過、停止位置、番線等の音声案内
- 列車番号、種別、行先、両数の表示
- 発車時刻表示
- 遅延表示
- 臨時停車・臨時通過
- 着発線変更
- 行先・種別・列車番号変更
- 途中駅での列情変更
- 有楽町線地下モード
- 右側ナビゲーション表示
- 録音 MP3 / 合成音声の切替
- iPadOS のバックグラウンド・スリープ復帰対策

### 最優先端末
**iPad が主対象。**
PC や Android で動くことより、iPadOS Safari / ホーム画面 Web アプリでの安定動作を優先する。

---

## 2. 技術構成

原則として以下を維持する。

- Vanilla JavaScript
- HTML
- CSS
- JSON / CSV
- ブラウザ標準 API
  - Geolocation
  - Wake Lock
  - HTMLAudioElement
  - SpeechSynthesis
  - fetch

フレームワーク、TypeScript、ビルドシステム、npm 依存等を勝手に導入しない。
大規模な構成変更は、ユーザーから明示的に依頼された場合のみ行う。

---

## 3. 現行版の扱い

リポジトリ内で実際に運用している `app.js`、`styles.css`、`index.html`、`data/` を現行版とする。

タイムスタンプ付きの旧版やバックアップが存在しても、それを基準に変更しない。
過去版は参照用であり、現行コードを巻き戻してはならない。

ユーザーが「この通りに修正しました」と報告した変更は、以降 **採用済み仕様** として扱う。
次の変更は、その採用済み状態を前提に積み上げること。

---

## 4. 変更時の絶対原則

### 4.1 関係ない機能を変えない
依頼された機能だけを修正する。
「ついでの整理」「コードをきれいにするための全面改修」「命名統一」「ファイル再編」は行わない。

### 4.2 既存挙動を先に読む
修正前に、対象関数だけでなく以下を確認する。

- 呼び出し元
- 呼び出し先
- state の読み書き
- タイマー
- Promise / キュー
- visibility / lifecycle
- GPS トリガー
- 同名関数・重複定義
- CSS 側の依存
- データファイル側の依存

### 4.3 安全側に勝手に仕様変更しない
このアプリでは距離、速度、方向、駅名、列車番号等が業務上の意味を持つ。
「一般的にはこの方が良い」という理由でトリガー値や案内文言を変更しない。

### 4.4 変更は最小差分
可能な限り既存関数を維持し、必要箇所だけを変更する。

### 4.5 ユーザーへコードを提示する場合
ユーザーがコピーペーストで修正する場合は、原則として **function 単位** で完全な置換コードを示す。
巨大な `app.js` 全体を本文へ貼らない。
前後のインデントを現行コードに合わせる。

### 4.6 完成ファイルを出力する場合
ユーザー環境では JS 単体のダウンロードに失敗した実績があるため、**ZIP 形式を優先**する。
必要なら JS 単体も併記するが、ZIP を主な受け渡し手段とする。

---

## 5. テスト・検証ルール

JavaScript を変更したら最低限:

```bash
node --check app.js
```

を実行する。

可能なら以下も確認する。

- `rg` / 検索で同名関数が重複していないか
- 変更した ID / class が JS と CSS で一致しているか
- 既存イベントハンドラが上書きされていないか
- Promise が永久待機しないか
- タイマーが終了時に解放されるか
- バックグラウンド復帰後に古いキューが残らないか

iPad 実機確認が必要な事項を、PC 上の静的解析だけで「動作確認済み」と断定しない。

---

## 6. 音声アーキテクチャ — 重要

### 現在の採用方式
音声方式は 2 種類。

- `recorded`
  - `data/voice/*.mp3`
  - **persistent HTMLAudioElement**
- `synthetic`
  - `SpeechSynthesis`

メニューの「音声変更」から切り替える。

### 録音音声で Web Audio API を使用しない
以前は `AudioContext` / `AudioBufferSourceNode` を使用した。
しかし iPadOS で、

- 別アプリへ切替
- 一時的なバックグラウンド移行
- 画面スリープ
- 復帰

の後に無音となる問題が繰り返し発生した。

そのため録音音声は **HTMLAudioElement を 1 個作り、使い回す方式**へ変更済み。

**理由なく Web Audio API 方式へ戻してはならない。**

### iPad 復帰時
バックグラウンド移行時:

- 録音音声を停止
- 待機中 Promise を解放
- `speechSynthesis.cancel()`
- 再許可が必要な状態を記録

フォアグラウンド復帰時:

- 案内中なら Wake Lock を再取得
- 「音声再開」ボタンを表示
- ユーザーの 1 タップで音声セッションを再許可

iPadOS の autoplay 制限を無視して自動再生を強行する設計へ戻さない。

### Audio Session
使用可能なら:

```js
navigator.audioSession.type = "transient-solo"
```

を優先し、失敗時は `"playback"` を試す現行設計。

---

## 7. 音声案内の保護事項

以下は不用意に変更しない。

- `speakOnce()` の同一キー 30 秒抑止
- 案内開始後 10 秒の通常案内抑止
- 地下モード中の通常音声抑止
- 音声停止ボタン
- 録音音声がない場合の合成音声フォールバック
- 音声キューの直列実行
- MP3 文節分割
- 固定複合文節の扱い

案内文言の完全一致が MP3 ファイル探索に影響するため、句読点・表記を「読みやすさ」だけで変えない。

---

## 8. データファイルの扱い

現在の主要データ:

- `data/stationdata.csv`
- `data/types.json`
- `data/destinations.json`
- `data/train_number_table.json`
- `data/car_icons.json`
- `data/platform.json`
- `data/nonpassenger_types.json`
- `data/voice/*.mp3`

### stationdata.csv
旧:

- `stations.json`
- `stationID.json`
- `station.csv`

の役割を統合済み。

`destinations.json` は **統合保留中**。
Codex の判断で `stationdata.csv` に統合しない。

### 文字コード
`stationdata.csv` は UTF-8 と Shift-JIS / CP932 の両方を読める設計を維持する。

### 既存データの形を変えない
列名、駅名、種別名、キー名を勝手にリネームしない。
データ移行は明示依頼時のみ。

---

## 9. API

現行コードは西武アプリ系の本番 API を直接参照している。

主なエンドポイント:

- `.../stations/{stationId}/departures`
- `.../trains?lineId=...`
- `.../lines/all/status`

API のキー揺れを吸収する既存コードがあるため、単一キー前提へ簡略化しない。

ネットワーク仕様や API 項目を変更する場合、既存のフォールバックを残す。

---

## 10. UI を壊さない

案内画面は左側 BAND1〜7 + 右側ナビ領域。

メニューは現在 **2 列**。
ただし:

- 「案内終了」
- 「とじる」

は横幅いっぱいの 1 列表示を維持する。

右側ナビ領域や BAND の比率は、依頼なしに再設計しない。

---

## 11. ルート・駅判定

主要ルート:

- 池袋線本線
- 有楽町線
- 豊島線
- 狭山線

分岐ロジック、route lock、共通駅扱いがある。
単純な「最寄り駅だけ」で全面置換しない。

特に:

- 練馬
- 西所沢
- 小竹向原
- 豊島園

周辺の処理は分岐ロジックと地下モードに直結する。

---

## 12. 作業報告

変更後は最低限、ユーザーへ次を説明する。

1. 原因
2. 変更した関数 / ファイル
3. 変更内容
4. 既存機能への影響
5. 実施した検証
6. iPad 実機で確認すべき項目
7. 未確認点 / リスク

「直しました」だけで終えない。

---

## 13. 禁止事項まとめ

ユーザーの明示指示なしに以下をしない。

- Web Audio API へ戻す
- stationdata.csv と destinations.json を統合する
- 大規模リファクタリング
- 新フレームワーク導入
- データファイル形式変更
- 距離 / 速度トリガー変更
- 特別案内文言変更
- メニュー配置の再設計
- ルート分岐ロジックの簡略化
- 本番 API を別サービスへ置換
- タイムスタンプ付き旧版を現行版へ巻き戻す
- ユーザーの採用済み修正を過去版で上書きする

詳細は `MUST_FOLLOW.md` と `HANDOFF.md` を参照すること。
