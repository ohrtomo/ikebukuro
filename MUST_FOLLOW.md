# MUST_FOLLOW.md

## 目的

この文書は、これまでの開発中にユーザーが「今後も守ってほしい」「この方針を基準にしてほしい」と指定した事項を、Codex へ引き継ぐためのものです。

これは単なる推奨ではありません。
仕様変更の依頼がない限り、以下を開発上の制約として扱ってください。

---

# A. ユーザーが明示した開発ルール

## A-1. 不足情報を勝手に補完しない

過去の明示ルール:

> 不足情報は質問

意味:

- コードを読めば判定できることは、まずコードを確認する。
- データを読めば判定できることは、まずデータを確認する。
- それでも仕様が複数解釈に分かれ、業務挙動が変わる場合は、推測で実装しない。
- 特に駅、方向、距離、列番、停車・通過、案内文言については勝手に補完しない。

ただし、単純なコード上の不具合で原因が明確な場合まで無用な質問を増やさない。

---

## A-2. 修正コードは function 単位

過去の明示ルール:

> 修正コードは function 単位

ユーザーが手作業で修正する場合:

- 変更対象関数を特定する
- 原則として関数全体を提示する
- 「どこからどこまで置換か」を明示する
- インデントを現行コードに合わせる
- 数行だけ変更する方が安全な場合は、検索対象となる既存コードも併記する

巨大な `app.js` 全文をチャット本文へ貼る方式は避ける。

Codex がリポジトリを直接編集する場合でも、ユーザーへの説明では「どの function をどう変えたか」を中心に報告する。

---

## A-3. 現行版への積み上げを維持する

ユーザーが:

> この通りに修正しました

と報告した変更は、その時点から採用済み。

以後:

- 旧ファイルをベースにしない
- 過去の提案を再度そのまま適用しない
- 既に変更済みの関数を旧版で上書きしない

必ず現在の `app.js` を読み、現行状態へ最小差分で追加する。

---

## A-4. 1日1回共有で採用継続

過去のプロジェクト運用ルールとして:

> 1日1回共有で採用継続

という指示がある。

この文言の実務上の扱い:

- ユーザーが共有した最新版・採用報告を、その後の基準版として継続する。
- 同日に複数版がある場合も、最も新しく「採用・修正済み」と確認された状態を優先する。
- 意味が曖昧な状況で独自の版管理ルールを作らず、必要ならユーザーへ確認する。

---

# B. プロジェクト固有の固定方針

## B-1. 主対象は iPad

このアプリは iPad で使うことを前提とする。

優先順位:

1. iPadOS Safari / ホーム画面 Web アプリ
2. その他モバイル
3. PC

iPad で壊れるが PC では動く、という状態を「問題なし」としない。

---

## B-2. 音声は現行の iPad 対策を維持

採用済み:

- 録音音声: persistent `HTMLAudioElement`
- 合成音声: `SpeechSynthesis`
- 録音 / 合成はメニュー「音声変更」から選択
- バックグラウンド移行時に音声を停止・破棄
- 復帰時は「音声再開」を表示
- ユーザー操作で再許可
- Wake Lock を復帰時に再取得

禁止:

- 理由なく Web Audio API (`AudioContext`) へ戻す
- 復帰時に autoplay 制限を無視して無限に `play()` / `resume()` を繰り返す
- バックグラウンド中に古い案内をキューへ積み続ける

---

## B-3. 音声モード

`state.config.voiceMode`

- `"recorded"` — 録音 MP3 優先。該当録音がなければその文節のみ合成音声へフォールバック
- `"synthetic"` — MP3 を使わず、案内文全体を合成音声で発話

この意味を変更しない。

---

## B-4. メニュー

ユーザー指定:

- 基本 2 列
- 「案内終了」は 1 列全幅
- 「とじる」は 1 列全幅

現行メニュー項目:

- 案内終了
- 臨時停車・通過
- 行先変更
- 種別変更
- 列番変更
- 音量設定・テスト
- 音声変更
- 地点リセット
- 運行情報
- 強制地下
- とじる

---

## B-5. destinations.json 統合は保留

`stationdata.csv` へ以下は統合済み:

- stations.json 相当
- stationID.json 相当
- station.csv 相当

しかし:

- `destinations.json`

の統合は **一旦見送り**。

勝手に再開しない。

---

## B-6. 手動設定の引継ぎ条件

対象:

- 臨時停車
- 臨時通過
- 着発線変更

案内終了後に再設定する場合:

**直前の列車番号と新しい列車番号が一致した場合だけ引き継ぐ。**

異なる列車番号へ無条件に引き継がない。

---

## B-7. 列番・方向

基本ルール:

- 偶数列番 = 上り
- 奇数列番 = 下り

途中駅列情変更では、変更前と変更後の列番は同一方向である必要がある。
現在は偶奇一致でチェックしている。

---

## B-8. 参照ボタン

設定画面の「参照」は現行仕様を維持する。

- 上り
- 下り
- 地下

地下:

- 最寄り GPS に依存せず小竹向原固定

上り / 下り:

- GPS 最寄り駅から候補を取得

一覧:

- 発車時刻順
- 列番
- 種別
- 行先

種別:

- `train_number_table.json` を優先

行先:

- API 側を使用

選択した列車番号を設定へ反映。

---

## B-9. 地下モード

地下モードでは通常の GPS 音声案内を抑止する。

上り小竹向原行き:

- 練馬停車後に `autoUndergroundReady`
- trains API の `toStationName` が `新桜台` または `小竹向原` になったら自動地下切替

下り地下起動:

- 開始画面の「地下起動」から開始可能
- 練馬約 210m 以内で地上 main へ戻す現行ロジックあり

この設計を単純な GPS 最寄り駅判定へ置き換えない。

---

# C. 音声案内文言 — 表記を勝手に変更しない

MP3 ファイル名判定にも影響するため、以下の現行文言を勝手に言い換えない。

- `案内を開始します。`
- `搭載かばん、確認`
- `運転停車、ドア扱い注意`
- `整列、確認`
- `ホームドア「S」確認`
- `列車無線切り替え`
- `列情変更`
- `方向幕確認`
- `列情確認`
- `ドア扱い注意`
- `停止位置注意`
- `速度注意`
- `着発線変更`
- `停車`
- `臨時停車`
- `通過`
- `臨時通過`
- `次は`

過去会話には一部異なる旧文言が存在する。
**現在の実コードの文言を優先する。**

特に旧案との差分:

- 現行: `列車無線切り替え`
  - 旧案の「列車無線チャンネル切り替え」へ戻さない
- 現行: `整列、確認`
  - 旧案の別文言へ戻さない
- 現行の椎名町トリガーは 220m 離脱
  - 旧 150m 案へ戻さない
- 現行の途中駅「列情確認」は到着後 20 秒
  - 旧 15 秒案へ戻さない

---

# D. 距離・速度トリガー — 無断変更禁止

現行の主な値:

- 発車後次駅案内: 190m 圏内 → 190m 圏外へ離れた瞬間
- 停車駅接近: 400m クロス
- 停止直前: 200m クロス
- 停車駅発車時刻表示: 190m 以内
- 通過案内1: 200m 以内 かつ速度 45km/h 以下
- 通過案内2: 120m 以内 かつ速度 30km/h 以下
- 練馬特別案内: 100m 圏内 → 圏外
- 所沢無線案内: 400m 以内
- 椎名町特別案内: 220m 圏内 → 圏外

200m 停止案内の速度条件は **撤廃済み**。
勝手に復活させない。

---

# E. データの固定事項

## E-1. stationdata.csv

主要列:

- 種類
- 名称
- 緯度
- 経度
- isStopover
- down8pos
- up8pos
- 所属路線1
- 所属路線2
- 所属路線3
- 駅ID
- それ以外の列 = 停車パターン

文字コード:

- UTF-8
- Shift-JIS / CP932

両対応を維持。

## E-2. 同名駅 / 複数路線の注意

駅 ID や所属路線には同名駅・複数路線の注意点がある。
過去に問題になった例:

- 萩山
- 東村山
- 小川
- 西武球場前

現在動作しているマッピングを、一般化のために勝手に作り直さない。
変更が必要なら、まず実データと API の駅 ID を照合する。

---

# F. UI の固定事項

## F-1. 案内画面

左側:

- BAND1: 両数 + 種別
- BAND2: 次停車駅等の案内表示
- BAND3: 列番 + 行先
- BAND4: 発車時刻
- BAND5: メニュー + 音声停止
- BAND6: 時計 + 遅延 + GPS
- BAND7: 空白 / 将来用

右側:

- 約 1/4
- ナビゲーション領域
- 赤丸現在位置
- 前後駅
- 現在駅
- 駅・踏切・信号・橋梁・トンネル等のスポット

右側スポット表示範囲は現在位置から半径約 600m。

---

# G. ファイル出力ルール

ユーザー環境で JS 直接ダウンロードが失敗した実績あり。

完成版を渡す場合:

1. ZIP を必ず用意
2. 必要なら JS 単体も用意
3. ZIP 内のファイル名を明示
4. 変更前後でどの版を基準にしたか明示

---

# H. 最終チェック

Codex は作業終了前に自問する。

- 現行 `app.js` を基準にしたか
- 過去版へ巻き戻していないか
- 関係ない機能を変えていないか
- iPadOS を最優先したか
- Web Audio を再導入していないか
- 距離 / 速度 / 文言を無断で変えていないか
- 手動設定引継ぎ条件を壊していないか
- 地下モードを壊していないか
- 途中駅列情変更を壊していないか
- `node --check app.js` を通したか
- 実機確認が必要な事項を明示したか
